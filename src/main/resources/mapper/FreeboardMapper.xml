<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.FreeboardMapper"> <!-- Mapper의 FQCN을 namespace으로 한다. -->
	<select id="selectByFreeboardid"
		resultType="com.example.model.Freeboard">
		SELECT * FROM freeboard where freeid=#{freeid}
	</select>
</mapper>

